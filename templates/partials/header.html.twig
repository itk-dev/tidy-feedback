<nav class="navbar navbar-expand-lg bg-body-tertiary mx-0">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('tidy_feedback_index') }}">
          <img class="logo" src="{{ path('tidy_feedback_asset', {asset: 'icons/tidy-feedback-logo.svg'}) }}">
        </a>
        <h1 class="m-0">{{ title }}</h1>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('tidy_feedback_index') }}">Home</a>
            </li>
        </ul>
    </div>
</nav>
